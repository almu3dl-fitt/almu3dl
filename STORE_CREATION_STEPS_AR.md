# خطوات إنشاء متجر رقمي مشابه لهذا المشروع

هذا الملف يحوّل العمل الموجود في المشروع إلى دليل عملي منظم لبناء متجر رقمي شبيه بمتجر `almu3dl`.
الترتيب هنا مبني على مراحل التنفيذ الفعلية، وليس على ترتيب الأيام.

## الهدف من المشروع

المتجر الحالي مبني لبيع منتجات رقمية مثل:

- برامج تدريب
- برامج غذائية
- باقات رقمية
- ملفات قابلة للتحميل بعد الشراء

وهذا يعني أن المشروع لا يحتاج شحنًا أو عناوين توصيل، بل يحتاج:

- عرض منتجات واضح
- سلة وشراء سريع
- دفع إلكتروني
- روابط تحميل آمنة
- حساب عميل
- لوحة إدارة

## 1. تجهيز المتطلبات الأساسية

قبل البدء، جهّز التالي:

- `Node.js`
- مشروع `Next.js` باستخدام `App Router`
- مشروع `Supabase`
- حساب `PayPal Business`
- حساب `Resend` لإرسال البريد
- دومين أو رابط تشغيل أساسي للموقع

الاعتمادات الأساسية الموجودة في المشروع:

- `next`
- `react`
- `@supabase/supabase-js`
- `@supabase/ssr`
- `resend`

## 2. إنشاء المشروع وتشغيله

ابدأ بإنشاء مشروع `Next.js` ثم ثبّت الاعتمادات الموجودة في `package.json`.

أوامر التشغيل الأساسية:

```bash
npm install
npm run dev
```

بعدها ابْنِ الهيكل الأساسي للصفحات:

- الصفحة الرئيسية
- صفحة المتجر
- صفحة تفاصيل المنتج
- صفحة السلة
- صفحة الدفع
- صفحة نجاح الطلب
- صفحات الحساب
- صفحات الإدارة

## 3. إعداد المتغيرات البيئية

هذا المشروع يعتمد على المتغيرات التالية:

```env
NEXT_PUBLIC_SITE_URL=

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

ADMIN_PASSWORD=

PAYPAL_CLIENT_ID=
PAYPAL_SECRET=
PAYPAL_MODE=sandbox

RESEND_API_KEY=
```

ملاحظات:

- `NEXT_PUBLIC_SITE_URL` يستخدم لبناء الروابط العامة وروابط التحميل.
- `SUPABASE_SERVICE_ROLE_KEY` مطلوب للعمليات الإدارية ورفع الملفات وإنشاء الروابط الموقعة.
- اسم مستخدم الإدارة الحالي ثابت داخل الكود، وإذا أردت تغييره عدّل `lib/admin-auth.ts`.
- `PAYPAL_MODE` يمكن أن يكون `sandbox` أو `live`.

## 4. إعداد `Supabase`

### الجداول الأساسية

أنشئ الجداول التالية لأنها مستخدمة فعليًا في المشروع:

### `categories`

الغرض:

- تصنيف المنتجات داخل المتجر

حقول أساسية مقترحة:

- `id`
- `name`
- `slug`

### `products`

الغرض:

- حفظ بيانات المنتجات الرقمية

حقول أساسية مستخدمة في المشروع:

- `id`
- `title`
- `description`
- `price`
- `is_free`
- `level`
- `slug`
- `category_id`
- `is_active`
- `tags`

### `product_files`

الغرض:

- ربط كل منتج بملفاته القابلة للتحميل

حقول أساسية:

- `id`
- `product_id`
- `file_name`
- `storage_path`
- `sort_order`

### `orders`

الغرض:

- حفظ بيانات الطلبات والمدفوعات

حقول أساسية:

- `id`
- `customer_email`
- `customer_name`
- `customer_phone`
- `amount`
- `status`
- `paypal_id`
- `created_at`

### `order_items`

الغرض:

- حفظ المنتجات التابعة لكل طلب

حقول أساسية:

- `id`
- `order_id`
- `product_id`
- `price_paid`

### `downloads`

الغرض:

- إنشاء روابط تحميل آمنة لكل ملف بعد الشراء

حقول أساسية:

- `id`
- `order_item_id`
- `product_file_id`
- `token`
- `expires_at`
- `download_count`
- `max_downloads`

### `email_leads`

الغرض:

- حفظ البريد الإلكتروني من السلة أو التحميلات المجانية

حقول أساسية:

- `email`
- `source`

### `product_reviews`

الغرض:

- استقبال تقييمات العملاء ومراجعتها قبل النشر

هذا المشروع يحتوي بالفعل على ملف migration لهذا الجدول:

- `supabase/migrations/20260321_create_product_reviews.sql`

### `Supabase Auth`

المشروع يستخدم `Supabase Auth` لحسابات العملاء.
المستخدم يُنشأ تلقائيًا بعد أول طلب ناجح أو أول طلب مجاني.

## 5. إعداد `Storage` في `Supabase`

أنشئ حاويتين رئيسيتين:

### `products`

تُستخدم لتخزين الملفات الرقمية القابلة للتحميل مثل:

- ملفات PDF
- ملفات البرامج
- أي ملفات يشتريها العميل

### `product-images`

تُستخدم لحفظ صور المنتجات التي تظهر داخل المتجر ولوحة الإدارة.

مهم:

- يجب حفظ الملفات بأسماء ومسارات آمنة.
- المشروع الحالي ينظم الملفات داخل مجلدات مرتبطة بمعرف المنتج.
- روابط التحميل النهائية تُنشأ كروابط موقعة وليست روابط عامة مباشرة.

## 6. بناء واجهة المتجر العامة

ابدأ ببناء الواجهة العامة قبل التعمق في الدفع.

الصفحات الأساسية في هذا المشروع هي:

- `/`
- `/store`
- `/store/[slug]`
- `/cart`
- `/checkout`
- `/success`

ما يجب تنفيذه:

- عرض المنتجات المفعّلة فقط
- دعم التصنيفات
- صفحة منتج تعرض التفاصيل والسعر والصور
- زر إضافة إلى السلة
- واجهة واضحة على الجوال
- محتوى تسويقي جيد في الصفحة الرئيسية

## 7. بناء نظام الصور للمنتجات

المشروع الحالي يدعم:

- صور مرفوعة حديثًا من لوحة الإدارة
- صور قديمة احتياطية لبعض المنتجات

خطوات التنفيذ:

1. اربط كل منتج بمجلد صور داخل `product-images`.
2. أنشئ أسماء ملفات آمنة ومرتبة.
3. اعرض الصور عبر روابط موقعة `signed URLs`.
4. أضف صورة بديلة إذا لم توجد صور حديثة.

النتيجة:

- واجهة منتج أجمل
- مرونة في الإدارة
- عدم كشف الروابط الأصلية للملفات مباشرة

## 8. بناء لوحة الإدارة

لوحة الإدارة في هذا المشروع تشمل:

- صفحة رئيسية للإحصاءات
- إدارة المنتجات
- إدارة الطلبات
- مراجعة التقييمات

خطوات التنفيذ:

1. أنشئ صفحة دخول للإدارة.
2. احفظ جلسة الإدارة في `cookie` آمنة.
3. أضف حماية للمسارات الإدارية عبر `proxy`.
4. أنشئ صفحات:
   - `/admin`
   - `/admin/products`
   - `/admin/products/new`
   - `/admin/products/[id]`
   - `/admin/orders`
   - `/admin/reviews`

ما يجب أن تدعمه لوحة الإدارة:

- إنشاء منتج
- تعديل منتج
- رفع ملفات المنتج
- رفع صور المنتج
- تفعيل المنتج أو إخفاؤه
- حذف الطلبات غير المكتملة عند الحاجة
- اعتماد التقييمات أو إخفاؤها أو حذفها

## 9. بناء إدارة المنتجات

هذه المرحلة من أهم مراحل المشروع.

عند إنشاء منتج جديد يجب حفظ:

- الاسم
- الوصف
- `slug`
- السعر
- هل هو مجاني أم لا
- المستوى
- التصنيف
- الوسوم
- حالة الظهور
- ملفات التحميل
- صور المنتج

يفضل أن تكون عملية الإنشاء بهذا الترتيب:

1. إنشاء سجل المنتج أولًا.
2. رفع ملفات التحميل وربطها بالمنتج.
3. رفع صور المنتج.
4. إعادة تحديث صفحات المتجر والإدارة.

## 10. بناء السلة

المشروع يحتوي على سلة شراء للمنتجات الرقمية.

المطلوب في هذه المرحلة:

- إضافة المنتج إلى السلة
- منع التكرار غير المرغوب
- تحديث العدد والإجمالي
- دعم الحذف من السلة
- إظهار حالة السلة في الواجهة

لأن المشروع رقمي، السلة أبسط من متاجر الشحن، فلا توجد:

- عناوين
- شركات توصيل
- تكاليف شحن

## 11. بناء صفحة الدفع

صفحة الدفع يجب أن تجمع:

- بيانات العميل الأساسية
- محتوى السلة
- إجمالي المبلغ
- وسائل الدفع

الحقول الأساسية المستخدمة هنا:

- الاسم الكامل
- البريد الإلكتروني
- رقم الهاتف

ويجب دعم حالتين:

- طلب مدفوع
- طلب مجاني

## 12. ربط `PayPal`

هذا المشروع يعتمد على `PayPal` كوسيلة دفع رئيسية.

التدفق المعتمد كالتالي:

1. العميل يضغط متابعة الدفع.
2. النظام يتحقق من المنتجات والأسعار من قاعدة البيانات.
3. النظام ينشئ طلبًا محليًا داخل جدول `orders` بحالة `pending`.
4. النظام يضيف عناصر الطلب داخل `order_items`.
5. النظام ينشئ طلبًا في `PayPal`.
6. بعد نجاح الدفع يتم تنفيذ `capture`.
7. يتم تحديث الطلب إلى `paid`.

نقاط مهمة:

- لا تعتمد على السعر القادم من الواجهة فقط.
- تحقّق من الأسعار داخل الخادم دائمًا.
- خزّن `paypal_id` داخل الطلب.
- عالج حالات الفشل مثل `failed` أو رفض البطاقة.

## 13. بناء تدفق الطلبات المجانية

هذا المشروع يدعم المنتجات المجانية أيضًا.

الفرق هنا:

- لا حاجة للمرور عبر `PayPal`.
- يتم إنشاء الطلب مباشرة بحالة `paid`.
- يتم إنشاء روابط التحميل فورًا.
- يتم إرسال البريد مباشرة.

هذه المرحلة مهمة لأنها تسمح بتقديم:

- ملفات مجانية
- عينات
- منتجات جذب

## 14. إنشاء روابط التحميل الآمنة

بعد نجاح الطلب يجب إنشاء صفوف داخل جدول `downloads`.

كل صف يمثل:

- ملفًا واحدًا
- مرتبطًا بعنصر طلب واحد
- له `token` خاص

عند فتح رابط التحميل يجب التحقق من:

- صلاحية الرابط
- أن الطلب مدفوع
- عدم تجاوز عدد التحميلات إن كان هناك حد

ثم:

- يُنشأ `signed URL` للملف من حاوية `products`
- يُزاد عداد التحميل
- يُعاد توجيه العميل إلى الملف

## 15. إرسال البريد بعد الشراء

بعد إتمام الطلب يجب إرسال بريد يحتوي على:

- ملخص الطلب
- اسم العميل
- المبلغ
- روابط التحميل

في هذا المشروع تم استخدام `Resend`.

يفضل أن يكون البريد:

- واضحًا
- مختصرًا
- متوافقًا مع العربية
- مناسبًا للجوال

## 16. إنشاء حساب العميل وتسجيل الدخول

المشروع الحالي يجعل تجربة العميل أكثر سلاسة عبر:

- إنشاء مستخدم في `Supabase Auth` بعد أول طلب ناجح
- تسجيل الدخول عبر رمز تحقق `OTP`
- إتاحة صفحة حساب تعرض الطلبات والتنزيلات

المراحل المقترحة:

1. أنشئ المستخدم تلقائيًا بعد أول شراء.
2. فعّل تسجيل الدخول بالبريد.
3. استخدم `OTP` بدل كلمة المرور إن أردت تجربة أبسط.
4. أنشئ صفحة `/account`.
5. أضف صفحة تفاصيل الطلب `/account/orders/[id]`.

## 17. بناء نظام التقييمات والمراجعات

لكي تجعل المتجر أكثر موثوقية:

1. اسمح بالتقييم فقط لمن اشترى المنتج.
2. اربط التقييم بالطلب والمنتج والبريد.
3. اجعل الحالة الافتراضية `pending`.
4. اعرض التقييم للعامة فقط بعد موافقة الإدارة.

الفائدة:

- منع التقييمات الوهمية
- تحسين الثقة
- إعطاء الإدارة تحكمًا قبل النشر

## 18. تحسين الصفحة الرئيسية وصفحات التسويق

بعد اكتمال الأساسيات، حسّن العرض التسويقي:

- إبراز المنتجات المميزة
- تحسين العناوين والوصف
- إضافة أقسام تعريفية
- إضافة عناصر بصرية للباقات
- دعم صفحات مساندة مثل حاسبة السعرات

هذه التحسينات لا تُنشئ المتجر من الصفر، لكنها ترفع التحويل وتجعل التجربة أكثر احترافية.

## 19. إعداد `SEO` والروابط العامة

نفّذ الأساسيات التالية:

- `robots.ts`
- `sitemap.ts`
- `metadata`
- عنوان ووصف واضحان للموقع
- روابط صحيحة مبنية على `NEXT_PUBLIC_SITE_URL`

## 20. حماية الإدارة والمسارات الحساسة

المطلوب هنا:

- منع الوصول إلى صفحات الإدارة بدون جلسة صحيحة
- حفظ الجلسة في `cookie` آمنة
- استخدام `proxy` لحماية المسارات
- منع تنفيذ واجهات `API` الإدارية لغير المصرح لهم

## 21. ترتيب التنفيذ المقترح من البداية إلى الإطلاق

إذا أردت بناء متجر مشابه من الصفر، فهذا هو الترتيب الأنسب:

1. إنشاء المشروع وتهيئة `Next.js`.
2. ربط `Supabase` وإعداد المتغيرات البيئية.
3. إنشاء الجداول الأساسية في قاعدة البيانات.
4. إنشاء `Storage` للملفات والصور.
5. بناء واجهات المتجر العامة.
6. بناء إدارة المنتجات داخل لوحة الإدارة.
7. بناء السلة.
8. بناء صفحة الدفع.
9. ربط `PayPal`.
10. إنشاء روابط التحميل بعد الدفع.
11. إرسال البريد.
12. إنشاء حساب العميل وصفحات الحساب.
13. إضافة التقييمات والمراجعة الإدارية.
14. تحسين الواجهة والهوية البصرية.
15. إعداد `SEO` والنشر النهائي.

## 22. قائمة تحقق قبل الإطلاق

قبل نشر المتجر، تأكد من الآتي:

- جميع المنتجات لها `slug` صحيح
- جميع المنتجات المباعة لها ملفات مرفوعة
- صور المنتجات تعمل
- الدفع يعمل على `sandbox` أولًا
- ثم يتم اختبار `live`
- الطلبات تُحفظ في قاعدة البيانات
- حالة الطلب تتغير من `pending` إلى `paid`
- البريد يصل بعد الشراء
- روابط التحميل تعمل
- العميل يستطيع تسجيل الدخول ورؤية طلباته
- لوحة الإدارة محمية
- المنتجات المخفية لا تظهر في المتجر

## 23. خلاصة تنفيذية

بناء متجر مشابه لهذا المشروع يمر عمليًا عبر خمسة محاور رئيسية:

1. قاعدة بيانات وملفات آمنة
2. متجر واضح للعميل
3. دفع موثوق
4. تسليم رقمي تلقائي
5. إدارة وتشغيل ومراجعة

إذا نُفذت هذه المحاور بهذا الترتيب، ستحصل على متجر رقمي قابل للبيع والإدارة والتوسع.
